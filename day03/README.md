## Day 3

Day 3 is all about if,else if and else commands and logical operators

## TASKS

-1. Task was to make a program that checks if a number is even or odd

Here we have an if command which cheks if the number we enterd is dividable by to or number%2 == 0. If it is than it is an even number if not is an odd number.

-2. Task was to write down the highest number out 3 numbers that user enters

Here we have 2 if commands. Each command checks if the number is higher than the previous number. We will see that h=x exist after the scanf. That is because we have to give a value to h and we check if it is higher or lower number than others. We have to give it that kind of a value and not 0. If the value is zero in case of 3 negative numbers entered we will not have a good result.

-3. Task was to make a program that checks if the entered year is a leap year

This one is based on 3 rules. If the number is dividible by 4 it is a leap year. If it is dividible by a 100 it is not a leap year. And if it is dividible by 400 it is a leap year. When you implement those rules into if commands the program will do what the task is asking for.

-4. Task was a sensor alarm

This was a little practice program in which the user enters a temperature and if it is higher than the limit(which i set to a 100) the messeage alarm appears else nothing happens.

## Day03 completed

This was a day where I learned about how conditions work in a program